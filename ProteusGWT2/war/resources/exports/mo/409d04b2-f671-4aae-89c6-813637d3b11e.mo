model one "description string"
Modelica.Electrical.Analog.Basic.HeatingResistor heatingresistor0 (useHeatPort = true,
T = 300.15,
R_ref = 1,
alpha = 0,
T_ref = 300.15) annotation(Placement(transformation(origin = {999, 136},
extent = {{-32, -32}, {32, 32}},
rotation = 0) ) );
annotation(Icon() ,
Diagram() ,
LinkMap() ,
RunConfigurations() ,
LibFiles = (""));
equation
end one;
