model MyModel "description string"
Modelica.Electrical.Analog.Basic.Ground ground0 () annotation(Placement(transformation(origin = [246, 148],
extent = [-32, -32, 32, 32],
rotation = 0) ) );
Modelica.Electrical.Analog.Basic.Resistor resistor0 (useHeatPort = false,
T = 300.15,
alpha = 0,
T_ref = 300.15,
R = 1) annotation(Placement(transformation(origin = [380, 129],
extent = [-32, -32; 32, 32],
rotation = 0) ) );
annotation(Icon() ,
Diagram() ,
LinkMap() ,
RunConfigurations() ,
LibFiles = (""));
equation
end MyModel;
